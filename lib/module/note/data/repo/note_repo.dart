
  //Example of Repo
  //You might not use it
  class NoteRepo {
  //   // final _myNetwork = NoteNetwork();
  
  //   // Every function should have repo as prefix
  
   Future<void> repoFunc() async {
      try {
  //      servFunc();
       } catch (e) {
         rethrow;
       }
     }
  
  //   Stream<Object> repoAnotherFunc() async* {}
  }
  